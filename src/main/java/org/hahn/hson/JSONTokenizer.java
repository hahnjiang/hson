package org.hahn.hson;

/**
 * Created by jianghan on 2016/11/12.
 */
public class JSONTokenizer {

    protected JSONObject tokenize(String line) {
        JSONObject obj = new JSONObject();
        for (int i = 0; i < line.length(); ++i) {
            char ch = line.charAt(i);
            switch (ch) {
            }
        }
        return obj;
    }

}
